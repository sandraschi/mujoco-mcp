[project]
name = "mujoco-mcp"
version = "0.1.0"
description = "General-purpose MuJoCo physics simulation via MCP"
requires-python = ">=3.11"
dependencies = ["fastmcp>=3.2", "mujoco>=3.2", "httpx>=0.28", "fastapi>=0.115", "uvicorn[standard]>=0.34"]

[project.scripts]
mujoco-mcp = "mujoco_mcp:main"
